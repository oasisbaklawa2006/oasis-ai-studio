VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Media governance: testing | pilot | production
# testing = hero only (default until catalogue pilot signoff)
VITE_MEDIA_GOVERNANCE_MODE=testing

# Dev-only: allow localStorage fallback for catalogue collections/versions (never enable in production)
# VITE_ALLOW_LOCAL_CATALOGUE_FALLBACK=true
